# 거제로(Geojero) — 프론트엔드 작업 컨텍스트

> 이 문서는 프론트엔드 작업자(사람 + AI)가 프로젝트 맥락 없이 투입되어도 바로 작업할 수 있도록 정리한 문서입니다.
> **작업 시작 전 전체를 읽어주세요.** 특히 "하지 말아야 할 것" 섹션은 이미 팀이 논의해서 배제하기로 결정한 항목들입니다.

---

## 1. 프로젝트 개요

**서비스명**: 거제로 (Geojero)
**한 줄 정의**: 대중교통(뚜벅이)으로 거제도 당일치기 여행이 **오늘 실제로 가능한지 판정**하고, 여행 중 버스를 놓쳤을 때 일정을 재조립해주는 서비스

**출품**: 2026 한국관광공사 관광데이터 활용 공모전 / 웹·앱 구현 부문
**지정과제**: 과제 1 — "여행 중 발생하는 실시간 변수(날씨 변화, 혼잡도 상승, **동선 꼬임** 등) 대응의 어려움"

### 핵심 문제의식

거제도는 연 700만 명이 찾는 국내 2위 섬이지만, 대중교통 정보가 신뢰할 수 없습니다.

- 네이버 지도는 **공식 시간표를 갖고 있지 않고 과거 도착 기록으로 추정**합니다
- 실제 대조 결과: 55번 노선 막차가 네이버에는 17:06, 공식 BIS에는 **19:15** — 2시간 차이
- 배차가 하루 5~7회인 지역에서 막차 정보 오류는 "불편"이 아니라 **여행 실패**입니다
- 기존 뚜벅이 여행 후기는 대부분 실패담이며, "면허를 따야겠다"로 끝납니다

### 서비스가 답하는 질문

> "어디가 좋은지"가 아니라 **"오늘 이 코스가 성립하는가"**

매력도 추천이 아니라 **시간표 데이터 기반 판정**입니다. 이 구분이 서비스 정체성의 핵심입니다.

---

## 2. 팀 · 일정 · 역할

| 항목 | 내용 |
|---|---|
| 인원 | 2명 (백엔드 1, 프론트 1) |
| 기간 | 약 2주 (1차 심사 제출 기준) |
| 프론트 | React + Vite, Vercel 배포 |
| 백엔드 | Spring Boot + PostgreSQL, EC2 + Docker + Caddy |

**심사 배점**: 서비스 구현성 30 / 기획력 30 / 데이터 활용 20 / 발전성 20
→ **구동 안정성과 완성도가 최우선**입니다. 화려한 기능보다 "에러 없이 끝까지 도는 것"이 점수입니다.

---

## 3. 현재 상태 (이미 완료된 것)

- 백엔드 배포 완료: `https://api.geojero.com`
- 헬스체크 동작 확인: `GET https://api.geojero.com/api/health` → `OK`
- 프론트 배포 완료: `https://geojero-client.vercel.app`
- **프론트 → 백엔드 CORS 통신 성공 확인됨**
- CI/CD: git push → GitHub Actions → GHCR → EC2 자동 배포

**아직 없는 것**: 실제 기능 API 전부 (판정, 타임라인, 재조립). 백엔드에서 병렬 개발 중.

---

## 4. 환경변수

`.env.local` (로컬) 및 Vercel 환경변수에 설정:

```
VITE_API_BASE_URL=https://api.geojero.com
VITE_KAKAO_MAP_KEY=<카카오 JavaScript 키>
```

**카카오 개발자 콘솔 등록 완료된 도메인**:
- `http://localhost:5173`
- `https://geojero-client.vercel.app`

---

## 5. 디자인 시스템

**레퍼런스**: 오늘의집(ohou.se) — 흰 배경 + 브랜드색 한 곳에만, 회색 라인 일러스트

### 컬러

```
Primary   #35C5F0   민트/시안 (CTA, 활성 상태, 성립 표시)
Ink       #2F3438   기본 텍스트
Sub       #8A9199   보조 텍스트
Line      #D3D9DE   테두리 · 라인 일러스트
Surface   #FFFFFF / #F4F6F8
Danger    #E5484D   불성립 · 경고 (막차 위험)
```

**원칙**: 화면 면적의 90%는 흰색/회색. 민트는 CTA·활성 탭·성립 표시에만.

### 타이포그래피

- 본문/UI: **Pretendard**
- 워드마크/제목: **나눔스퀘어라운드 ExtraBold** (선택)
- 숫자는 `font-variant-numeric: tabular-nums` 적용 (시각·요금 정렬)

### 아이콘

- **Lucide** 라인 아이콘 사용
- **이모지 사용 금지** (AI가 만든 티가 남)

---

## 6. 화면 구조 (확정)

화면은 **3개**입니다. 이 이상 늘리지 마세요.

### ① 메인 — 지도 (첫 진입 화면)

```
┌──────────────────────────────────────┐
│ 부산서부 · 9/7 · 07:00 → 23:00   ▾   │  ← 상단 요약 칩 (탭 → 화면②)
├──────────────────────────────────────┤
│                                      │
│         카카오맵 (거제 전체)          │
│    ● 해금강     ● 바람의언덕          │  ← 민트 핀 = 성립
│    ○ 여차홍포                        │  ← 회색 핀 = 불성립
│                                      │
│   ┌────────────────────┐            │
│   │ 코스 요약 카드       │            │  ← 핀 탭했을 때만 등장
│   │ 2h10m · 막차 21:20  │            │
│   │ [자세히 보기]        │            │
│   └────────────────────┘            │
└──────────────────────────────────────┘
```

- **앱 최초 진입 시 이미 판정된 상태로 지도가 뜹니다.** 빈 폼이나 조건 입력 화면이 먼저 나오면 안 됩니다
- 기본값: 오늘 날짜 / 마지막 선택 터미널(localStorage) / 가장 빠른 첫차
- 평소엔 지도가 전체 화면. 하단 카드는 핀을 탭했을 때만 등장
- 불성립 코스도 회색으로 표시합니다 → "다 된다고 하지 않는 앱"이 신뢰를 만듭니다

### ② 판정 조건 입력 (전체 화면, 상단 칩 탭 시 진입)

이미 디자인 완료된 화면입니다. 구성:

- **출발지**: 검색창 + 칩(부산서부 / 서울남부 / 통영). "시간표 있음 · 판정에 포함" 배지
- **날짜 · 출발 시각**: 날짜 선택 + 출발 시각
- **귀가 시각**: 기본값 "막차까지 (제한 없음)", 사용자가 조일 수 있음
- **코스 테마 필터**: 전체 / 언덕·전망 / 유람선 / 해수욕장 / 식물원 / 성
- **코스 카드 가로 캐러셀**
- 하단 CTA: **판정하기**
- 안내 문구: "로그인 없이 판정됩니다"

### ③ 코스 상세 — 역산 타임라인 (시그니처 화면)

이 화면이 서비스의 핵심 UI입니다.

```
상단: 코스 요약 (이동 시간 / 체류 시간 / 예상 요금)
────────────────────────────────────
타임라인 (위 → 아래로 시간 순)
  08:40  고현터미널 도착
   ↓ 시내버스 55번 (60분)
  10:30  바람의언덕      90분
   ↓ 도보 12분
  13:00  점심            60분
  ...
  ┌ 여유 시간 40분 ┐  ← 점선 블록, 버퍼 시각화
────────────────────────────────────
하단 앵커 (고정, 민트 배경):
  "여기서부터 거꾸로 짰어요"
  20:00 부산행 버스 탑승     (진짜 막차 21:20 · +80분 여유)
```

**핵심 UI 개념 — 역산 타임라인**:
- 화면 **하단에 막차 앵커가 고정**되어 있고, 그 위로 일정 블록이 쌓입니다
- 블록 사이 **점선 여백 = 버퍼(늦어도 되는 시간)**
- 거제는 앵커가 **2개**일 수 있습니다: 유람선 막배(외도 등) + 부산행 막차
- 재조립 시 **하단 앵커는 고정된 채 위쪽 블록만 재배열**됩니다 → 이 대비가 "역산"을 시각적으로 증명

---

## 7. Trip Mode (여행 당일) — 후순위

시간이 남으면 구현. 구성:

- 상단 고정 카운트다운: `막차까지 4h 12m`
- 현재 스텝 하이라이트
- 하단 상시 버튼: **[버스를 놓쳤어요]**
- 버튼 탭 → 재조립 API 호출 → 타임라인 재배열 + 배너 `"20:00 막차, 아직 안전해요"`

**이 재조립 장면이 심사 데모의 하이라이트입니다.** 과제 1("동선 꼬임")의 직접적 구현체이므로, 우선순위가 밀리더라도 최소한 화면은 만들어야 합니다.

---

## 8. 하지 말아야 할 것 (팀 결정 사항)

이미 논의해서 **배제하기로 확정된 항목**들입니다. 다시 제안하지 마세요.

| 항목 | 이유 |
|---|---|
| **GPS / 현재 위치 표시** | 사용자는 거제가 아닌 부산·서울에 있음. 필요한 건 "출발 터미널"이고 그건 선택하게 하는 게 정확함 |
| **왕복/편도 필터** | 편도(1박)를 허용하면 막차 앵커가 사라져 서비스 정체성이 무너짐. MVP는 당일 왕복 전용 |
| **자차 모드** | MVP는 대중교통 전용. 로드맵으로만 언급 |
| **길찾기 직접 구현** | 카카오맵 딥링크로 위임. 우리는 "시간"을 소유하고 "길"은 카카오가 그림 |
| **예매 기능** | 외부 예매처(버스타고 등) 딥링크로 위임 |
| **후기 / 배지 / 프로필 꾸미기** | 콜드 스타트 문제. 추가 기능으로 분리 |
| **다국어** | MVP는 한국어만 |
| **localStorage 외의 상태 저장** | 로그인은 "코스 저장" 시점에만 요구. 판정·타임라인은 게스트로 전부 열람 가능 |

---

## 9. API 명세 (백엔드 개발 중 — 목 데이터를 이 구조로 만들 것)

> **중요**: API가 아직 없으므로 **목 데이터로 화면을 먼저 만드세요.**
> 단, 목 데이터를 JSX 안에 하드코딩하지 말고 **파일 상단에 아래 구조의 객체로 분리**해두면 나중에 fetch 결과로 교체만 하면 됩니다.

### ① 코스 목록 + 판정 (메인 지도용)

```
GET /api/courses?origin=BUSAN_SEOBU&date=2026-09-07&departTime=07:00&returnBy=23:00
```

```json
{
  "arrivalTime": "08:40",
  "courses": [
    {
      "courseId": 1,
      "name": "부산발 당일치기 · 해금강",
      "theme": "NATURE",
      "thumbnailUrl": "https://...",
      "region": "남부권",
      "feasible": true,
      "reason": null,
      "lat": 34.7912,
      "lng": 128.6634,
      "travelMin": 130,
      "stayMin": 390,
      "returnAnchorTime": "20:00",
      "lastBusTime": "21:20",
      "bufferMin": 80,
      "estimatedCost": 41200
    },
    {
      "courseId": 3,
      "name": "여차홍포 전망 코스",
      "theme": "NATURE",
      "feasible": false,
      "reason": "주말 배차 감축으로 당일 왕복 불가",
      "lat": 34.7521,
      "lng": 128.5893
    }
  ]
}
```

- `feasible` → 핀 색상 결정 (true: 민트, false: 회색)
- `reason` → 불성립 시 카드에 표시할 사유 문구
- 불성립 코스는 나머지 필드가 null일 수 있음

### ② 코스 상세 — 역산 타임라인

```
GET /api/courses/{id}/timeline?origin=BUSAN_SEOBU&date=2026-09-07&departTime=07:00&returnBy=23:00
```

```json
{
  "courseId": 1,
  "courseName": "부산발 당일치기 · 해금강",
  "feasible": true,
  "anchors": [
    { "type": "LAST_FERRY", "time": "16:00", "label": "외도 막배" },
    { "type": "LAST_BUS",   "time": "21:20", "label": "부산행 막차" }
  ],
  "boardingTime": "20:00",
  "bufferMin": 80,
  "blocks": [
    {
      "seq": 1, "type": "MOVE",
      "from": "부산서부", "to": "고현터미널",
      "startTime": "07:00", "endTime": "08:40",
      "routeNo": "시외"
    },
    {
      "seq": 2, "type": "MOVE",
      "from": "고현터미널", "to": "도장포",
      "startTime": "09:20", "endTime": "10:20",
      "routeNo": "55"
    },
    {
      "seq": 3, "type": "VISIT",
      "poiId": 12, "name": "바람의언덕",
      "startTime": "10:30", "endTime": "12:00",
      "stayMin": 90,
      "imageUrl": "https://...",
      "accessTier": "GOOD"
    },
    {
      "seq": 4, "type": "MEAL",
      "name": "점심",
      "startTime": "13:00", "endTime": "14:00",
      "stayMin": 60
    }
  ]
}
```

- `blocks[].type`: `MOVE`(이동) / `VISIT`(관광) / `MEAL`(식사) — 타입별로 블록 UI를 다르게 렌더링
- `anchors`: 하단 고정 앵커. 배열이므로 2개일 수 있음

### ③ 재조립 (Trip Mode)

```
POST /api/courses/{id}/rebuild
{ "missedAt": "16:50", "currentPoiId": 12, "origin": "...", "date": "...", "returnBy": "..." }
```

응답은 ②와 동일한 구조 + 추가 필드:
```json
{
  "isSafe": true,
  "message": "20:00 버스, 아직 안전해요"
}
```

---

## 10. 지금 할 작업

**카카오맵 JavaScript SDK 연동 + 메인 지도 화면 구현**

1. 카카오맵 SDK 스크립트 로드 (`VITE_KAKAO_MAP_KEY` 사용)
2. 거제도 중심 좌표로 지도 렌더링 (대략 `lat: 34.88, lng: 128.62`, level 9 정도에서 시작해 조정)
3. 목 데이터의 코스 좌표에 **커스텀 마커** 표시
   - `feasible: true` → 민트(#35C5F0) 마커
   - `feasible: false` → 회색 마커
4. 마커 클릭 → 하단에 코스 요약 카드 표시
5. 상단에 요약 칩(`부산서부 · 9/7 · 07:00 → 23:00`) 배치 — 탭 시 판정 조건 화면으로 이동(라우팅만 잡아두고 화면은 이후)

**참고**:
- 지도는 피그마에서 목업이 불가능한 라이브 컴포넌트이므로, **코드에서 직접 보면서 작업**하는 것이 맞습니다
- 카카오맵은 기본 마커 대신 `CustomOverlay`를 쓰면 HTML/CSS로 마커를 자유롭게 디자인할 수 있습니다
- 확대/축소 시 마커가 겹칠 수 있으니 초기 level 조정 필요

---

## 11. 작업 원칙

- **오버엔지니어링 금지.** 2명이 2주 안에 끝내야 합니다. 상태관리 라이브러리, 복잡한 폴더 구조, 추상화 레이어를 미리 만들지 마세요. `useState`로 충분합니다
- **완성도 > 기능 수.** 심사 배점상 "에러 없이 도는 것"이 최우선입니다
- **모바일 우선.** 심사위원이 폰으로 열어볼 가능성이 높습니다. 반응형은 모바일 기준으로 잡고 데스크톱은 중앙 정렬 컨테이너로 처리
- **PWA 수준까지만.** 네이티브 앱 전환은 범위 밖입니다
- **의사결정이 필요하면 물어보세요.** 임의로 기능을 추가하거나 배제된 항목을 되살리지 마세요
