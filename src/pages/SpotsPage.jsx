import PlaceholderScreen from '../components/PlaceholderScreen'

/** 스팟 — 개별 관광지 목록. 다음 작업입니다. */
export default function SpotsPage() {
  return (
    <PlaceholderScreen
      tab
      title="스팟"
      lead="개별 관광지를 훑어보는 화면입니다. 아직 안 만들었습니다."
    />
  )
}
